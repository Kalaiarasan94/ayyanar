# ============================================================
#  BACKEND ENVIRONMENT CONFIG
#  Keep ONE block active at a time — comment the other with #
#  This file is git-ignored; it never gets pushed to GitHub.
# ============================================================

# ---------------- LOCAL DEVELOPMENT (XAMPP) ----------------
# Active now. Default XAMPP MySQL: user root, empty password.
#DB_HOST=localhost
#DB_PORT=3306
#DB_USER=root
#DB_PASSWORD=
#DB_NAME=construction_erp
#PORT=5000

# ---------------- PRODUCTION (HOSTINGER) ----------------
# When deploying: comment the LOCAL block above, uncomment below,
# and fill in the values from hPanel -> Databases -> MySQL Databases.
# (On Hostinger you can also set these in the panel's Environment
# Variables section instead of uploading this file.)
# NOTE: do NOT set PORT in production — Hostinger assigns it automatically.

DB_HOST=localhost
DB_PORT=3306
DB_USER=u928398901_ayyanar
DB_PASSWORD=Kirtheesh@123
DB_NAME=u928398901_ayyanar
PORT=5000
